##############################################
# Punt - Environment Configuration
##############################################

##############################################
# REQUIRED: Solana Program Configuration
##############################################
# Your deployed Punt program ID on Solana
NEXT_PUBLIC_PROGRAM_ID=your_program_id_here

# Solana RPC endpoint (devnet, testnet, mainnet-beta)
NEXT_PUBLIC_NETWORK=https://api.devnet.solana.com


##############################################
# REQUIRED: Database (Neon PostgreSQL)
##############################################
# PostgreSQL connection string with SSL
# Get from: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require


##############################################
# REQUIRED: Arcium Encryption Keys
##############################################
# Generate these keys using: ./scripts/generate-arcium-keys.sh
# KEEP THESE SECRET! Never commit to git!

# Solana keypair for paying Arcium transactions (base58 encoded)
ARCIUM_PAYER_SECRET_KEY=your_base58_encoded_payer_secret_key

# x25519 keypair for client-side encryption (base58 encoded)
ARCIUM_CLIENT_SECRET_KEY=your_base58_encoded_client_secret_key

# Arcium MXE Program ID (for future on-chain encrypted storage)
ARCIUM_MXE_PROGRAM_ID=3gaXj1oSXKqn9rTgcPahqU9z3L2fjYexKYpmU1xNhefL

# Solana RPC for Arcium operations
ARCIUM_SOLANA_RPC_URL=https://api.devnet.solana.com


##############################################
# OPTIONAL: LiveKit Streaming
##############################################
# Only required if using live streaming features
# Get credentials from: https://cloud.livekit.io

# LiveKit WebSocket URL
LIVEKIT_URL=wss://your-livekit-instance.livekit.cloud

# LiveKit API credentials
LIVEKIT_API_KEY=your_api_key
LIVEKIT_API_SECRET=your_api_secret

# Chat message to freeze polls
FREEZE_CHAT_MESSAGE=🧊 Poll frozen by host


##############################################
# OPTIONAL: Public Arcium Configuration
##############################################
# These are safe to expose to the frontend

NEXT_PUBLIC_MXE_PROGRAM_ID=3gaXj1oSXKqn9rTgcPahqU9z3L2fjYexKYpmU1xNhefL
NEXT_PUBLIC_ARCIUM_NETWORK=https://api.devnet.solana.com
NEXT_PUBLIC_ARCIUM_MXE_NAME=punt-bet-records


##############################################
# Setup Instructions
##############################################
# 1. Copy this file to .env.local
#    cp .env.example .env.local
#
# 2. Generate Arcium keys:
#    ./scripts/generate-arcium-keys.sh
#
# 3. Fill in DATABASE_URL from Neon console
#
# 4. Fill in NEXT_PUBLIC_PROGRAM_ID after deploying Punt program:
#    cd ../punt-program
#    anchor build
#    anchor deploy --provider.cluster devnet
#
# 5. (Optional) Add LiveKit credentials for streaming
#
# 6. Run database migrations:
#    npx prisma migrate deploy
#    npx prisma generate
#
# 7. Start development server:
#    npm run dev
##############################################

